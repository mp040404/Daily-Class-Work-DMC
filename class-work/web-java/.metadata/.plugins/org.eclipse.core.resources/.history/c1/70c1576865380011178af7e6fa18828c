package com.example.demo;

import java.util.List;
import java.util.Scanner;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.CommandLineRunner;
import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class Sb12datajpaApplication implements CommandLineRunner {

	public static void main(String[] args) {
		SpringApplication.run(Sb12datajpaApplication.class, args);
	}
	
	@Autowired
	private BookDao bookDao;

	@Override
	public void run(String... args) throws Exception {
		Scanner sc = new Scanner(System.in);
		//------------------------------------------------------------
		/*
		  List<Book> list = bookDao.findAll();
		for (Book book : list) {
			System.out.println(book);	
		}
		*/
		//------------------------------------------------------------
		/*
		System.out.println("Enter Category you want to find it");
		String cat = sc.nextLine(); 
		List<Book> list = bookDao.findByCategoryOrderByPriceDesc(cat);
		for (Book book : list) {
			System.out.println(book);
		}
		*/
		//-------------------------------------------------------------
		/*
		System.out.println("Enter author you want to find it");
		String cat = sc.nextLine();
		List<Book> list = bookDao.findByAuthor(cat);
		for (Book book : list) {
			System.out.println(book);
		}
		*/
		//--------------------------------------------------------------
		/*
		List<Book> list = bookDao.findByPriceBetween(300, 400);
		for (Book book : list) {
			System.out.println(book);
		}
		*/
		//----------------------------------------------------------------
		/*
		System.out.println("Enter Category you want to update price 10% increase it");
		String cat = sc.nextLine(); 
		int cnt = bookDao.incBookPriceByCategory(1.1, cat);
		System.out.println(cnt);
		*/
		//----------------------------------------------------------------
		System.out.println("Categorys in present in table Books");
		List<String> list = bookDao.findUniqueCategories();
		for (String string : list) {
			System.out.println(string);
		}
	}

}
